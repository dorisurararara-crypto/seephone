# Codex Auto-Start Instructions

This repository uses `new인수인계.md` as the canonical operating memory.

MUST at the start of every Codex session in this repo:

1. Read `new인수인계.md` before planning or editing.
2. Follow its `SESSION_START Gate`.
3. Use its Rule Hierarchy when any instruction conflicts.
4. If the task has an active playbook, read that playbook before work.
5. Keep Codex as planner/reviewer and Claude CLI as code/data editor unless the user explicitly overrides or emergency recovery requires a minimal direct fix.
6. Do not delete or revert files unless the user explicitly approves. Preserve unrelated dirty files.

Current active playbook:

- `docs/operating_memory/celebrity_db_playbook.md`

If this file and `new인수인계.md` conflict, `new인수인계.md` is the detailed source of truth unless a newer user/system/developer instruction says otherwise.

